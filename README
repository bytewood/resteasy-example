A simple web application that includes maven plugins to create a keystore and implement ssl in tomcat.

USAGE:
mvn clean
mvn package
./run.sh (on linux)
./run.bat (on windows)

urls:
    https://localhost:8443/resteasy-server/resources/things
    https://localhost:8443/resteasy-server/resources/things/person

